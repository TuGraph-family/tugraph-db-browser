{
  "name": "tugraph-db-browser",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "licenses": [
    {
      "type": "Apache-2.0",
      "url": "https://github.com/TuGraph-family/tugraph-db/blob/master/LICENSE"
    }
  ],
  "scripts": {
    "dev": "pnpm -r --stream run dev",
    "client-bootstrap": "cd client && yarn install --ignore-engines",
    "server-bootstrap": "cd server && yarn install --ignore-engines",
    "bootstrap": "yarn run client-bootstrap && yarn run server-bootstrap",
    "client-dev": "cd client && yarn run dev",
    "server-dev": "cd server && yarn run dev",
    "server-start": "cd server && yarn run start",
    "start": "pnpm -r --stream run start",
    "client-build": "cd client && yarn run build",
    "server-build": "cd server && yarn run build",
    "sycnfile": "cd server && yarn run sycnfile",
    "build": "yarn run client-build && yarn run server-build && yarn run sycnfile"
  },
  "repository": "https://github.com/TuGraph-family/tugraph-db-browser.git"
}
